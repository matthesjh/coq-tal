language: generic

os: osx

before_install:
  - brew update
  - brew install coq

script:
  - coqc -v
  - coq_makefile -f _CoqProject -o Makefile
  - make

notifications:
  email:
    on_success: never
    on_failure: change

branches:
  only:
    - master

git:
  depth: 10